global-include *.py
global-include *.c
global-include *.md
global-include *.yaml
global-include *.yml
global-include Makefile
include LICENSE

# NOTE: shared library must be included
# When the module is installed with 'pip install', it is built in a temporary 
# directory and THEN are copied to 'site-packages' 
global-include *.so